version: '3.4'
services:
  prysm-beacon-chain-nate.public.dappnode.eth:
    image: 'prysm-beacon-chain-nate.public.dappnode.eth:0.1.0'
    build: ./build
    volumes:
      - 'data:/data'
    ports:
      - '4000:4000'
    restart: always
volumes:
  data: {}
